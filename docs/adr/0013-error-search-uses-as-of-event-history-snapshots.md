# 错误检索使用冻结时点的事件历史快照

错误历史在翻页和查看详情期间仍会继续产生或恢复，若直接读取当前投影会使列表、分面和证据互相矛盾。决定由 Host 在首个请求冻结 ErrorSearchAsOf，并从 DemandSeriesEvent 按该时点重建列表、状态、分面和详情；游标与后续详情绑定同一时点，新事件只在用户刷新后出现，以历史查询与索引复杂度换取一次检索内的稳定解释。

**Status**: accepted
