# 资格审计使用独立的投影快照身份

CatalogRevision 只在外部可读目录成员或成员值变化时递增，始终不可读的 Demand 即使阻断原因改变也可能不触发目录修订，因此它不足以冻结资格审计的列表、分面和详情。决定让资格审计绑定一个投影提交快照，并把该提交对应的 CatalogRevision 作为相关目录事实返回；分页、计数、原因集合和详情共享审计快照，刷新才进入新提交。

**Status**: accepted
