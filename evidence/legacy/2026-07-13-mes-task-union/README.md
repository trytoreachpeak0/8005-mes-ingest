# 2026-07-13 五类任务旧 runner 证据

本目录保存旧 runner 的单次质量、五客户分支行数对比和 10 轮性能结果。摘要记录
641 行、平均 3.13 秒、最大 4.37 秒，但原始 CSV 实际只有 6 列，缺少
`PACKAGE`，且没有当前 bundle/run manifest 格式。

该证据只证明当时旧查询和旧工具的执行情况，不得由 `import-run` 晋升为 latest，
也不得证明当前 7 字段 `MES_TASK_UNION` 已通过复验。
